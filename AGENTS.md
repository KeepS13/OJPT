# OJPT Agent Working Rules

本文件约束所有在本仓库内工作的 agent。默认目标是保护文档源文件、交付文件和真实项目实现之间的一致性，尤其是论文 Markdown、合并版、DOCX、参考文献、图表之间的协同更新。

## 适用范围

- 本规则适用于仓库根目录及全部子目录。
- 如后续在子目录新增更具体的 `AGENTS.md`，子目录规则在其范围内优先。
- 用户没有明确限定只处理单个文件时，默认按“源文件与交付件一起更新”的方式处理文档。

## 事实来源

- 修改功能说明、接口说明、部署说明前，必须先核对真实项目文件，优先查看 `OJPT-backend`、`OJPT-frontend`、`docker-compose.yml`、`.docs/功能说明书/**`。
- 不得凭记忆或推测编造接口、页面、角色、数据库字段、部署能力、运行端口或业务流程。
- 如果文档与代码不一致，先指出差异并基于真实实现修正文档；除非用户明确要求改代码，否则不要用文档反推实现。

## 论文协同更新

- 默认把 `.docs/论文/OJPT-论文-合并版.md` 和对应章节 Markdown 视为论文长期内容源，DOCX 视为最终交付件。
- 修改论文正文时，必须同步更新对应章节 Markdown、`.docs/论文/OJPT-论文-合并版.md` 和最终 DOCX。
- 禁止只改 DOCX、只改合并版、只改章节 Markdown、只改参考文献而不更新相关引用位置，除非用户明确要求临时只改某一个文件。
- 如果用户直接要求修改 DOCX，先把变更落回对应 Markdown 源文件，再同步到 DOCX；不得把 DOCX 当作唯一长期源。
- 修改摘要、目录、章节标题、图表编号、引用编号、术语名称时，要同步检查所有受影响文件，避免同一概念出现多套表述。

## 参考文献与引用

- 修改 `.docs/论文/90-参考文献.md` 时，必须同步全文引用编号、相关章节、合并版 Markdown 和 DOCX。
- 涉及 `[16]`-`[20]` 时要额外谨慎，默认核对英文文献、知网可查、直接 URL 可人工检查等约束。
- 不得只替换文末参考文献列表而遗漏正文中的引用编号、引用语境和对应论述。
- 给用户汇报参考文献调整时，应说明被替换条目、引用位置、核查方式和仍需人工确认的链接。

## 图表与截图

- 新增、删除或替换论文图表时，必须同步图片文件、正文图号、图题、`.docs/论文/92-图表引用清单.md`、合并版 Markdown 和 DOCX。
- 优先复用 `.docs/论文/assets/**` 和 `.docs/功能说明书/assets/**` 中已有素材。
- 截图、流程图、ER 图、架构图必须能对应真实功能或真实项目结构，不得使用与实现不符的示意图充数。

## DOCX 操作

- 不得打开 Word 手工编辑 DOCX。优先用 `python-docx`、`lxml`、`zipfile` 等方式做离线脚本化修改。
- 修改 DOCX 前必须保留备份，备份文件名应能看出时间或用途。
- 修改后必须检查 DOCX 包完整性，例如 `testzip() == None`。
- 修改后必须检查章节结构、标题层级、图表编号、引用编号、关键术语和残留旧文本。
- 如果 DOCX 渲染或格式风险较高，应额外做页面导出或结构化检查，并在最终回复中说明验证结果。

## Review 输出

- 做论文或文档审阅时，问题列表按“内容差异、歧义、语法、格式、引用/证据”分桶。
- 审阅意见必须给出具体文件、章节或可定位的上下文，不只给概括性总结。
- 发现文档与代码、截图、图表、参考文献不一致时，优先列为需要修复的问题。

## 编码与命令

- 中文 Markdown 和中文文件名按 UTF-8 处理。
- PowerShell 输出乱码不能直接判断为文件损坏；必须用正确编码重新读取或用脚本验证。
- 搜索文件和内容优先使用 `rg` / `rg --files`。
- 不要覆盖、回滚或清理用户已有改动，除非用户明确要求。

## 验证要求

- 论文正文修改后：核对章节 Markdown、合并版 Markdown、DOCX 三者内容一致。
- 参考文献修改后：核对参考文献列表、正文引用编号、合并版 Markdown、DOCX 和 URL 可核查性。
- 图表修改后：核对图片文件、图号、图题、图表清单、正文引用和 DOCX 渲染位置。
- 功能文档修改后：核对后端接口、前端路由/API 调用、部署配置，并在最终回复中说明依据文件。
