---
alwaysApply: true
---
# Skill 使用与对话流程规则（OJPT）

- **语言与风格**
  - 所有回答默认使用简体中文。
  - 回答应简洁、信息密度高，除非用户明确要求详细展开。

- **文件编辑**
 - 在创建新文件或大块代码前，应先**预估本次生成的代码行数**。
   - 如果单个生成块预计**超过 300 行代码**，必须优先选择“先创建空文件 / 基础骨架”，再按模块或功能分批（多次调用）补全内容，避免因网络或上下文截断导致文件只生成一半。
   - 对于需要一次性生成的大文件（如长配置、文档），优先按章节/模块拆分为多个文件或多次生成，再在本地合并。

- **测试**
 - 默认不使用 Java 编写新测试代码；**优先使用 TypeScript（TS）** 编写测试。
   - 后端相关逻辑如需测试，优先通过前端或 API 层的 TS 测试（例如使用 Vitest、Playwright 等）来覆盖，而不是新增 JUnit/Java 单测。
   - 在编辑测试的时候，如不清楚测试账号数据等，可以从后端 OJPT-backend\src\main\resources\db flyway数据库文件中查看，在开发阶段，基本数据与flyway记录一致，就算修改，在测试的时候也需要回撤，以防数据不一致问题出现，账户密码默认为123456

- **任务开始时的 Skill 选举（必须执行）**
  - 每当用户派发一个新的任务或提出一个需要编码/设计/调试的问题时，必须先判断该任务所属的主要领域：
  - 后端 Java/Spring Boot 相关：优先考虑使用 `springboot-patterns`、`springboot-security`、`springboot-tdd` 以及本仓库 `.cursor/skills` 目录下的 Spring Boot 4+ 相关 skill。
    - 前端 Vue/Element Plus/测试相关：优先考虑使用 `vue`、`element-plus-vue3`、`vitest-testing-patterns`、`web-design-guidelines` 等 skill。
    - 通用工程实践/TDD 相关：优先考虑使用 `test-driven-development` skill。
  - 在开始具体实现或给出长篇方案之前，助手必须显式地在内部评估是否需要调用这些 skill，并据此决定是否读取对应的 `SKILL.md` 并遵循其中流程。

- **Skill 安装与归档约定（统一管理）**
  - 当助手通过 `npx skills add` 或其他方式为本项目安装新的 skill 时，应在安装完成后，将对应 skill 目录从 `.agents/skills`（或其他默认安装位置）复制到当前仓库的 `.cursor/skills` 目录下，统一以 `.cursor/skills` 作为本项目的权威 skill 源。
  - 在复制到 `.cursor/skills` 并确认可正常使用后，应默认移除 `.agents`、`.claude`、`.kiro` 等路径下同名的项目级 skill 目录，避免同一 skill 在多个位置重复配置造成混乱。
  - 在选择/调用 skill 时，优先查找和使用 `.cursor/skills` 目录下的版本；如需偏离（例如为了兼容全局配置），需在回复中用一句话说明原因。

- **不使用 Skill 时的要求**
  - 如果针对某个任务决定不调用任何 skill，助手应在回复开头用一句简短中文自然语言总结原因（例如：问题只是简单问答、不涉及代码修改，或 skill 不适用当前场景）。
  - 这句话只需面向用户可读，不需要提及内部实现细节。

- **使用 Skill 后的行为**
  - 一旦为某个任务调用了与之匹配的 skill，后续同一任务中的所有操作应尽量遵循该 skill 中给出的流程与最佳实践（例如 TDD 步骤、分层设计方式、测试模式等）。
  - 如需偏离 skill 中建议的做法，应在回复中用一句话说明偏离原因（例如：项目现有实现风格不同、与现有代码冲突、或用户明确另有偏好）。

- **任务粒度与持续性**
  - 把“任务”理解为用户的一段连续目标（例如：实现某个接口、完成某个页面、设计一段部署配置），在该目标完成前，应视为同一个任务，复用已选定的 skill 组合。
  - 当用户显式切换到全新主题或新模块时，应重新进行一次 Skill 选举流程。
  - 每次对代码进行较为完整的功能级更新（包括新增功能、修改已有业务逻辑、重要重构），都必须同步更新 `.cursor/doc/PROJECT-PLAN.md` 中对应功能项的状态、测试情况与是否需要调优等字段，确保文档与实现保持一致。
  - 若一次改动涉及多个功能，则应在同一次提交中一并更新相关 plan 条目，并将“是否已更新 PROJECT-PLAN.md”作为自查项。

- **大整数 ID 精度**
  - 所有由雪花算法生成的长整型 ID（例如 `problemId`、`submissionId`、`userId` 等），在前端与接口层必须统一使用字符串类型传递，禁止以 JS 数值类型传递，以避免因精度丢失导致各类“资源不存在”等 404 问题（实为 ID 被四舍五入或截断）。

- **本地运行环境检查与端口规范**
  - 在执行需要联调或运行代码的任务前，应主动检查前后端服务是否已启动：
    - 前端使用默认端口 `5173`；
    - 后端使用默认端口 `8080`。
    - 后端有安装DevTools可以热启动。
  - 如发现对应服务未启动，应自行按项目约定在上述默认端口启动前端与后端服务。
  - 不允许同一服务在非默认端口二次启动；如检测到前后端服务运行在其他端口，应优先关闭该异常进程，并确保仅在 `5173`（前端）与 `8080`（后端）上保留一份正常运行的实例。
  - 如项目使用 Nginx 作为前端/后端代理或静态资源网关，在上述检查中还需确认 Nginx 进程已正常启动并加载当前项目所需配置；如未启动或配置异常，停止目前工作，提示用户开启本机的nginx，再进行联调与访问。
  - 如果是因为flyway报错，大概率是因为数据库被修改，停止任务，告知用户操作一下数据库
