## OJPT 项目功能备忘录（全项目）

> 目的：用一份文档快速回答“这个项目有哪些模块、现在做到哪、怎么做的、怎么验证的、下一步是什么”。  
> 维护原则：**实现/修复/联调跑通后就更新**（状态 + 验证方式 + 关键文件/命令）。  
> 状态口径：以 `.cursor/doc/PROJECT-PLAN.md` 为准（UA/BK/JD/TUN 编号）。本文件偏“速查 + 经验沉淀”。

### 快速入口（常用地址/命令）

- **后端**：`http://localhost:8080`（Swagger：`/swagger-ui/index.html`；OpenAPI：`/v3/api-docs`）
- **前端（dev）**：`http://localhost:5173`
- **Nginx 联调入口**：`http://localhost`（`/api` 反代到后端）
- **常用命令**：
  - 前端启动：`cd OJPT-frontend && npm run dev`
  - 前端单测：`cd OJPT-frontend && npm run test:unit`
  - 前端 E2E：`cd OJPT-frontend && npm run test:e2e`
  - 后端启动：`cd OJPT-backend && .\\mvnw.cmd spring-boot:run`

---

## 1) 后端模块（OJPT-backend）

### 用户与权限中心（UA-xxx）

#### 已完成

- **UA-002 用户登录/登出（已完成）**
  - **怎么完成的**：Spring Security 6 + JWT（Access/Refresh）+ Redis 存储（refresh token + blacklist），统一返回结构。
  - **怎么验证的**：Playwright 登录流程用例 + TS API 单测覆盖关键分支（登录失败/未登录访问等）。
  - **关键文件**：
    - `OJPT-backend/src/main/java/com/example/ojpt/security/*`
    - `OJPT-frontend/src/stores/auth.ts`
    - `OJPT-frontend/tests/e2e/login.spec.ts`

- **UA-003 JWT 认证与续期（已完成/前端部分实现）**
  - **怎么完成的**：refresh token 轮换 + 旧 refresh token 失效（黑名单/存储），后端完成核心逻辑，前端续期流程部分接入。
  - **怎么验证的**：接口错误分支覆盖（如 refreshToken 缺失）+ 联调冒烟。
  - **关键文件**：
    - `OJPT-backend/src/main/java/com/example/ojpt/security/RefreshTokenStore.java`
    - `OJPT-backend/src/main/java/com/example/ojpt/security/JwtService.java`

#### 正在完成

- **UA-001 用户注册（进行中）**
  - **现状**：前后端均“部分实现”，表单校验与错误提示尚未统一；测试待落地。
  - **下一步**：补齐注册接口契约、表单校验与错误提示规范；补 TS 单测与 E2E。

- **UA-004 用户角色管理（进行中）**
  - **现状**：RBAC 框架已具备，更多场景与规则仍在补齐；已有参数校验测试覆盖部分管理接口。
  - **下一步**：梳理角色/权限变更的边界（越权、禁用态、批量操作）并补测试。

- **UA-005 组织管理：学校/院系/班级（进行中）**
  - **现状**：接口与部分联调已做，管理端 UI 仍需完善（列表/树状结构与操作体验）。
  - **下一步**：明确 UI 信息架构（学校 → 院系 → 班级）与权限边界；补齐组件测试。

- **UA-006 用户资料编辑与头像上传（进行中）**
  - **现状**：后端上传接口已实现；前端与测试尚需补齐（删除/替换/权限/路径对齐）。
  - **下一步**：Nginx `/uploads` 与后端上传路径全链路确认；补 TS 测试。

- **UA-007 基础统计与后台看板（进行中）**
  - **现状**：后端已部分实现，Playwright 有 `/admin` 看板基础冒烟；前端看板功能未完全落地。
  - **下一步**：统计口径与性能预估；必要时加缓存/预聚合（见 TUN-03）。

---

### 题库与题目管理（BK-xxx）

#### 已完成

- **BK-001 题目 CRUD（已完成）**
  - **怎么完成的**：后端题目查询/详情/标签关联等 API 已就绪；并按“匿名可看列表/详情、提交必须登录”配置安全策略。
  - **怎么验证的**：页面联调（题库列表/详情可匿名访问）+ 题号路由详情可直达。
  - **关键文件**：
    - `OJPT-backend/src/main/java/com/example/ojpt/controller/ProblemController.java`
    - `OJPT-backend/src/main/java/com/example/ojpt/service/impl/ProblemServiceImpl.java`
    - `OJPT-backend/src/main/java/com/example/ojpt/config/SecurityConfig.java`
    - `OJPT-frontend/src/views/ProblemSetView.vue`
    - `OJPT-frontend/src/views/ProblemSolveView.vue`

- **BK-003 题目展示编号 problemNo（已完成）**
  - **怎么完成的**：数据库新增 `problem_no`（自增/唯一），前端展示为 `P0001`；详情路由切换为 `/problems/{problemNo}`；后端新增按题号取详情 `GET /api/problems/no/{problemNo}`；旧 `/problems/id/:id` 路由已移除。
  - **怎么验证的**：
    - 后端：`curl http://localhost:8080/api/problems/no/1` 返回 200
    - 前端：`http://localhost:5173/problems/1` 可渲染且不显示长雪花 id
    - 旧路由：`/problems/id/...` 返回 404（开发阶段不保留兼容）
  - **关键文件**：
    - `OJPT-backend/src/main/resources/db/migration/V1_2__init_problem_bank.sql`
    - `OJPT-backend/src/main/java/com/example/ojpt/entity/Problem.java`
    - `OJPT-backend/src/main/java/com/example/ojpt/controller/ProblemController.java`
    - `OJPT-frontend/src/router/index.ts`
    - `OJPT-frontend/src/api/problem.ts`

#### 正在完成

- **BK-002 标签与难度管理（进行中）**
  - **现状**：后端标签与绑定接口已实现；前端管理界面未实现。
  - **下一步**：补后台标签管理页（列表/创建/绑定）与组件测试。

---

### 提交与评测（JD-xxx）

#### 正在完成

- **JD-001 提交与判题（stub 阶段）（进行中）**
  - **怎么完成的（当前阶段）**：提交落库 + 状态更新 + 进度表（尚未接真实判题/队列）。
  - **怎么验证的**：TS API 单测已覆盖 submission API；E2E 仍需补更完整链路。
  - **关键文件**：
    - `OJPT-backend/src/main/java/com/example/ojpt/controller/SubmissionController.java`
    - `OJPT-backend/src/main/java/com/example/ojpt/service/impl/SubmissionServiceImpl.java`
    - `OJPT-frontend/src/api/submission.ts`
    - `OJPT-frontend/tests/e2e/problem-submission.spec.ts`

#### 规划中 / 未开始

- **JD-002 多语言支持与资源限制（未开始）**
  - **前置依赖**：判题沙箱/队列/资源隔离方案（Docker/worker/队列等）；需要单独里程碑拆解。

---

## 2) 前端模块（OJPT-frontend）

### 学员端：题库列表/做题页

#### 已完成

- **题库列表页接后端（BK-001 对应）**
  - **怎么完成的**：列表展示题号 `Pxxxx`、标题/难度/标签；点击跳转 `/problems/{problemNo}`。
  - **怎么验证的**：匿名访问 `http://localhost:5173/problemset` 可看到题目并进入详情。
  - **关键文件**：`OJPT-frontend/src/views/ProblemSetView.vue`

- **做题页题面 Markdown 渲染（已完成）**
  - **怎么完成的**：`marked` 解析 + `DOMPurify` 清洗；隐藏题面首个 H1（避免与 title 重复）；样式用 `:deep()` 作用到 `v-html` 内容。
  - **怎么验证的**：详情页可展示段落/列表/代码块，且无重复标题。
  - **关键文件**：
    - `OJPT-frontend/src/utils/markdown.ts`
    - `OJPT-frontend/src/views/ProblemSolveView.vue`
    - `OJPT-frontend/tests/unit/utils/markdown.spec.ts`

- **编辑器面板高度随窗口变化按分割比例填满（已完成）**
  - **怎么完成的**：`ResizeObserver` 监听父容器高度变化；按当前 `splitRatio` 动态计算上下两块卡片高度。
  - **怎么验证的**：调整窗口高度/拖动分割线，编辑器与测试用例区保持占满并按比例变化。
  - **关键文件**：`OJPT-frontend/src/views/ProblemSolveView.vue`

#### 正在完成 / 待优化

- **登录体验一致性（见 TUN-01）**
  - **现状**：提交未登录会弹登录框（已满足核心要求），但错误提示/Loading/失败反馈仍可统一。
  - **下一步**：统一消息提示策略与按钮 loading、错误码到 UI 的映射。

---

### 前端基础设施（API/路由/状态）

#### 已完成

- **API 封装与大数 id 精度保护（已接入）**
  - **怎么完成的**：统一 `axios` 封装（baseURL、错误处理、token 注入），Snowflake id 在 TS 层按 string 传输避免精度问题。
  - **怎么验证的**：E2E 提交用例曾因 JS number 精度导致 404，改为 string 后恢复正常。
  - **关键文件**：`OJPT-frontend/src/api/request.ts`、`OJPT-frontend/tests/e2e/problem-submission.spec.ts`

---

## 3) 部署与联调（OJPT-deploy / Nginx）

#### 已完成

- **Nginx：静态资源 + `/api` 反代 + `/uploads` 静态**
  - **怎么完成的**：配置集中在 `OJPT-deploy/nginx/nginx.conf`，用于本地模拟生产入口。
  - **怎么验证的**：`nginx -t` + `nginx -s reload`；浏览器通过 `http://localhost` 访问前端、`/api` 访问后端。
  - **关键文件**：`OJPT-deploy/nginx/nginx.conf`
  - **补充**：前端生产构建会输出到 Nginx `html`（见 `OJPT-frontend/vite.config.ts` 与 `OJPT-frontend/README.md` 的 outDir 说明）。

#### 已验证

- **SPA history 路由刷新/直达（已手动验证通过）**
  - **验证结果**：`/problemset`、`/problems/1` 刷新或直达均不出现 404，正常回退到 `index.html`。
  - **相关点**：Nginx 配置 `try_files $uri $uri/ /index.html;` 在生产构建 + Nginx 环境下已确认有效。

---

## 4) 测试体系（TS-only）

#### 已完成

- **Vitest 单元测试（API/工具）**
  - **怎么完成的**：对 `src/api/*`、`src/utils/*` 建立 TS 单测；覆盖 problemNo API、submission API、markdown 渲染与 XSS 清洗。
  - **怎么验证的**：`cd OJPT-frontend && npm run test:unit`
  - **关键文件**：
    - `OJPT-frontend/tests/unit/api/problem.spec.ts`
    - `OJPT-frontend/tests/unit/api/submission.spec.ts`
    - `OJPT-frontend/tests/unit/utils/markdown.spec.ts`

- **Playwright E2E（关键链路）**
  - **怎么完成的**：登录、题目提交等链路用例；E2E 中通过后端登录拿 fresh token，避免 refresh token 黑名单导致 401。
  - **怎么验证的**：`cd OJPT-frontend && npm run test:e2e`
  - **关键文件**：
    - `OJPT-frontend/tests/e2e/login.spec.ts`
    - `OJPT-frontend/tests/e2e/problem-submission.spec.ts`
    - `OJPT-frontend/tests/e2e/problem-no.spec.ts`
    - `OJPT-frontend/tests/e2e/problem-pagination.spec.ts`（题库分页 total/pages/size 回归用例）
    - `OJPT-frontend/tests/e2e/user-banned-login.spec.ts`（封禁账号登录提示“剩余时间”）

---

## 5) 调优与风险跟踪（TUN-xx）

- **TUN-01 登录体验（已实现）**：已统一登录错误提示（含错误密码/网络异常/封禁剩余时间），并补 Playwright E2E 覆盖封禁登录提示（`user-banned-login.spec.ts`）
- **TUN-02 JWT 续期（待处理）**：状态边界复杂（多端/过期/并发刷新），建议补状态机与更多测试
- **TUN-03 统计性能（规划中）**：预估大数据量风险，考虑预聚合/缓存/异步刷新

---

## 6.1) 近期修复记录（高影响 Bugfix）

- **题库分页 total/pages=0（已修复）**
  - **现象**：`GET /api/problems` 返回 `records` 有数据，但 `total/pages` 为 0，导致前端分页条/总数显示异常。
  - **原因**：自定义 `SqlSessionFactory` 未装配 MyBatis-Plus 分页拦截器。
  - **修复**：在后端 `MybatisConfig` 装配 `MybatisPlusInterceptor + PaginationInnerInterceptor(DbType.MYSQL)` 并注入 plugins。
  - **怎么验证的**：`GET /api/problems?page=1&size=1` 返回 `total>=records.length` 且 `pages>=1`（非空时）；Playwright `problem-pagination.spec.ts` 已覆盖回归。
  - **关键文件**：
    - `OJPT-backend/src/main/java/com/example/ojpt/config/MybatisConfig.java`
    - `OJPT-frontend/tests/e2e/problem-pagination.spec.ts`

---

## 6) 本周/近期完成记录（用于快速回忆）

- 题库/详情页改为 **题号路由**：`/problems/{problemNo}`；后端新增 `GET /api/problems/no/{problemNo}`
- **移除**旧 id 路由：`/problems/id/:id`（开发阶段不保留兼容）
- 题面 **Markdown 渲染 + XSS 清洗**；隐藏首个 H1；样式用 `:deep()` 生效
- 做题页编辑器面板：随窗口变化按分割比例动态填满

---

## 7) Roadmap（未开始/规划中模块，占位但持续维护）

> 这部分用于“全项目视角”，避免只记当前已做的，回头不知道还缺哪些大模块。细化编号与状态以 `PROJECT-PLAN.md` 为准。

- **训练 / 作业与竞赛（规划中）**
  - **子模块**：训练计划、作业发布、竞赛创建/报名、排行榜/封榜策略、赛后统计
  - **前置依赖**：评测链路稳定（JD-001→真实判题）、权限模型完善（UA-004/005）

- **学习分析与报表（规划中）**
  - **子模块**：个人画像、班级/学校统计、题目难度与通过率分析、导出报表
  - **风险点**：统计口径统一 + 性能（TUN-03）

- **运维与系统管理（规划中）**
  - **子模块**：配置中心/参数管理、操作审计日志、监控告警、健康检查与发布流程
  - **现状**：Nginx 入口已具备；Actuator/OpenAPI 已接入（后端 README 有路径约定）


